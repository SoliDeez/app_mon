apiVersion: 1
providers:
  - name: 'All dashboards'
    orgId: 1
    folder: 'All dashboards'
    type: 'file'
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
